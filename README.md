## Target
The project is used to create an environment for running laravel framework inside with docker.
You can deploy your application on a vps soon with the docker image.

## Usage
Run `make build` to build the docker image locally.

Run `make run` to run a docker container with the image.

